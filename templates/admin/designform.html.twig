{% extends "admin/base.html.twig" %}

{% block body %}

    

	{{form_start(designForm)}}

	{{form_label(designForm.name, 'Nom')}}
	{{form_widget(designForm.name)}}

	{{form_label(designForm.country, 'Pays d\origine')}}
	{{form_widget(designForm.country)}}

	{{form_widget(designForm.Enregistrer)}}

	{{form_end(designForm)}}

{% endblock %}